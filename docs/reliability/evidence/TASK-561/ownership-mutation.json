{
  "name": "check-and-swap verification disabled: a concurrent writer's file is silently replaced",
  "mutations": [
    {"file": "Sources/DiskStewardApp/AgentIntegrations/AgentIntegrationInfrastructure.swift",
     "replacements": [["        let replaced = try? Data(contentsOf: temporary)\n        if replaced != expected {\n", "        let replaced = try? Data(contentsOf: temporary)\n        if false, replaced != expected {\n"]]}
  ]
}
