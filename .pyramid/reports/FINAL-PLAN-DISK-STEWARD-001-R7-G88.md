<!-- Generated by Pyramid Task V3. -->
# Final Report: Disk Steward macOS evidence monitor

- Plan: `PLAN-DISK-STEWARD-001`
- Revision: `7`
- Graph version: `88`
- Completed at: `2026-09-12T16:55:32.277653Z`
- Completed by: `codex`

## Intent

A macOS user can leave Disk Steward running as a lightweight menu-bar app, understand material disk growth, export trustworthy evidence for Codex or Claude, and query the same evidence through a read-only local MCP integration.

## Success Evidence

- `REQ-001` — A signed-development build launches as a menu-bar utility; left-click opens a compact status board and right-click exposes Export, Settings, About, and Quit. (covered by: GATE-190, GATE-290, GATE-390, GATE-490, INTENT-001, OUTCOME-010, OUTCOME-020, RESEARCH-101, TASK-110, TASK-112, TASK-212)
- `REQ-002` — The app measures whole-volume capacity while collecting bounded detailed evidence for configured roots and registered agent process trees. (covered by: CONTRACT-101, CONTRACT-401, GATE-290, GATE-390, GATE-490, INTENT-001, OUTCOME-020, OUTCOME-040, TASK-210, TASK-211, TASK-212, TASK-310, TASK-410)
- `REQ-003` — Evidence identifies path, time, size delta, actor and agent session when observable, and labels exact, tool-linked, inferred, or unknown attribution without guessing. (covered by: CONTRACT-101, CONTRACT-301, CONTRACT-401, GATE-290, GATE-390, GATE-490, INTENT-001, OUTCOME-020, OUTCOME-030, OUTCOME-040, RESEARCH-401, TASK-210, TASK-211, TASK-213, TASK-310, TASK-312, TASK-410, TASK-411)
- `REQ-004` — A user can export a portable, integrity-manifested evidence bundle containing a concise Codex brief and machine-readable detailed records. (covered by: CONTRACT-101, CONTRACT-301, GATE-190, GATE-290, GATE-390, GATE-490, INTENT-001, OUTCOME-010, OUTCOME-020, OUTCOME-030, TASK-111, TASK-112, TASK-213, TASK-311)
- `REQ-005` — Codex and Claude can query sanitized evidence through the same read-only local MCP executable without rescanning the disk. (covered by: CONTRACT-301, GATE-390, GATE-490, INTENT-001, OUTCOME-030, RESEARCH-101, TASK-310, TASK-311, TASK-312, TASK-411)
- `REQ-006` — Monitoring remains useful without restricted privileges and enables exact Endpoint Security provenance only when entitlement and user authorization are available. (covered by: CONTRACT-401, GATE-290, GATE-490, INTENT-001, OUTCOME-040, RESEARCH-401, RISK-420, TASK-211, TASK-212, TASK-410, TASK-411)
- `REQ-007` — The app records no file contents, provides explicit privacy exclusions and retention limits, and performs no deletion or cleanup mutation in this intent. (covered by: CONTRACT-101, CONTRACT-301, CONTRACT-401, GATE-190, GATE-290, GATE-390, GATE-490, INTENT-001, OUTCOME-010, OUTCOME-020, OUTCOME-030, OUTCOME-040, RESEARCH-401, RISK-420, TASK-111, TASK-112, TASK-210, TASK-211, TASK-212, TASK-213, TASK-310, TASK-311, TASK-312, TASK-410, TASK-411)
- `REQ-008` — Automated and reproducible checks demonstrate buildability, evidence correctness, regression safety, bounded idle resource use, and recovery from unavailable monitoring components. (covered by: CONTRACT-101, CONTRACT-301, CONTRACT-401, GATE-190, GATE-290, GATE-390, GATE-490, INTENT-001, OUTCOME-010, OUTCOME-020, OUTCOME-030, OUTCOME-040, RESEARCH-101, RESEARCH-401, RISK-420, TASK-110, TASK-111, TASK-112, TASK-210, TASK-211, TASK-212, TASK-213, TASK-310, TASK-311, TASK-312, TASK-410, TASK-411)

## Verified Primary Nodes

- `INTENT-001` — Trustworthy macOS storage evidence for humans and agents
- `OUTCOME-040` — Advanced trustworthy provenance
- `OUTCOME-030` — Agent-queryable storage evidence
- `OUTCOME-020` — Persistent bounded evidence recorder
- `OUTCOME-010` — Launchable disk snapshot and export utility
- `RESEARCH-101` — Validate native toolchain and packaging path
- `CONTRACT-101` — Define core evidence and privacy contracts
- `TASK-110` — Create the native project foundation
- `TASK-111` — Implement volume snapshot and basic export
- `TASK-112` — Implement menu-bar shell and compact status board
- `GATE-190` — Audit launchable snapshot increment
- `TASK-210` — Implement bounded SQLite evidence store
- `TASK-211` — Implement hybrid monitoring and growth explanation
- `TASK-212` — Add background lifecycle, settings, status, and notifications
- `TASK-213` — Implement complete evidence bundle export
- `GATE-290` — Audit persistent evidence recorder increment
- `CONTRACT-301` — Define agent session and read-only MCP contracts
- `TASK-310` — Implement agent session registry and process correlation
- `TASK-311` — Implement the bundled read-only MCP connector
- `TASK-312` — Package Codex and Claude integration paths
- `GATE-390` — Audit agent-queryable evidence increment
- `RESEARCH-401` — Validate Endpoint Security feasibility and fallback boundary
- `CONTRACT-401` — Define privileged event bridge and provenance semantics
- `TASK-410` — Implement optional Endpoint Security observer
- `TASK-411` — Implement exact and inferred provenance engine
- `RISK-420` — Enforce privacy, resource, permission, and release controls
- `GATE-490` — Audit advanced provenance and final intent

## Residual Risks

- RESEARCH-101: No valid code-signing identity is installed, so Developer ID distribution, notarization, and production system-extension signing are not currently demonstrable.
- RESEARCH-101: xcodebuild -showsdks emitted FSEvents/cache warnings in the automated shell even though it completed successfully; TASK-110 must verify whether project builds are affected.
- RESEARCH-101: Endpoint Security entitlement availability remains external and must preserve the standard unprivileged fallback.
- CONTRACT-101: The local Swift test validator intentionally implements only the JSON Schema keywords used by the version-1 contracts; adding new schema keywords requires extending the validator or adopting a maintained validator dependency.
- CONTRACT-101: Python jsonschema is not installed in the repository environment, so it was not counted as passing evidence; jq syntax validation and executable Swift contract tests are the recorded checks.
- TASK-110: The first sandboxed disposable-copy attempt was blocked by nested sandbox/module-cache restrictions; the identical clean-copy commands passed outside that restriction, so this is recorded as harness behavior rather than a product defect.
- TASK-110: The current app target is an intentionally invisible accessory-process skeleton. TASK-112 must provide the first user-visible status item and interaction lifecycle.
- TASK-110: An Xcode app/system-extension project is still required before privileged packaging; its exact form remains deferred and must not be inferred from the successful SwiftPM build.
- TASK-111: Whole-volume collection intentionally omits inaccessible or non-local volumes and records metadata failures as limitations; detailed directory consumption remains outside this first-slice task.
- TASK-111: The current manifest records full mount paths. Later privacy settings must support basename or hashed detail before exporting detailed file evidence.
- TASK-111: Product version is an explicit 0.1.0 default until packaging supplies a release version.
- TASK-112: SwiftPM launches an executable rather than a distributable signed .app bundle; packaging and login-item behavior remain later work.
- TASK-112: The first-slice export action writes to Documents/Disk Steward Exports and reveals the result in Finder; user-selectable destinations belong in later settings work.
- TASK-112: This node intentionally shows only the current capacity snapshot; history, thresholds, and cleanup controls remain out of scope.
- GATE-190: The desktop was locked during gate execution, so direct menu-bar clicking was unavailable; the retained native offscreen render and independent live launch/accessibility probe cover the rendered and runtime halves without weakening either check.
- GATE-190: The first increment is a SwiftPM development executable, not yet a signed distributable app bundle.
- GATE-190: The live status board currently displays the accessible path used to query the data volume; path redaction settings are scheduled for the evidence/export increment.
- TASK-210: The system-library module deliberately links Apple SDK libsqlite3 rather than a Homebrew build so the macOS 13 deployment target remains valid.
- TASK-210: Hard size-ceiling enforcement may evict the oldest unreviewed anomaly only after normal age-based compaction is insufficient; forced eviction count is surfaced in RetentionReport.
- TASK-210: Direct database access remains internal to the app-owned core; MCP must consume a later sanitized service boundary.
- TASK-211: FSEvents is intentionally notification-only and cannot establish creator-process identity; metadata rescans derive byte deltas with inferred confidence.
- TASK-211: Whole-volume allocation can change outside configured roots, so residual growth is exported as unexplained rather than attributed speculatively.
- TASK-211: FSEvents drop, wrap, mount, unmount, and root-change flags force unknown confidence and an explicit rescan limitation.
- TASK-211: External volumes are sampled for aggregate capacity only when returned by the existing volume service; file-level monitoring remains limited to configured roots.
- TASK-212: SMAppService.mainApp registration requires a properly bundled application; unit tests verify adapter semantics while signed-bundle behavior remains a packaging observation for the increment gate.
- TASK-212: User notification authorization is requested only when a threshold crossing first needs delivery, and denial does not interrupt evidence collection.
- TASK-212: A targeted FSEvents failure leaves scheduled whole-volume and configured-root sampling active and surfaces an explicit degraded state.
- TASK-212: Settings changes restart the targeted stream; the sampling interval itself takes full effect on the next loop iteration.
- TASK-213: Hourly and daily rollup tables do not retain consumer category, so category breakdowns cover raw detail while rollup totals remain path/operation structured evidence.
- TASK-213: Rollup buckets may overlap an exact requested-range boundary; the bundle states this limitation instead of implying per-event precision.
- TASK-213: Zlib was chosen for deterministic in-process compression; consumers must decompress events.jsonl.zlib before reading JSON Lines.
- TASK-213: The integrity manifest hashes every evidence payload, while manifest.json carries the integrity file's hash and is not self-hashed.
- GATE-290: The 0.37-second resource observation covers launch/smoke only; long-duration idle and degraded component budgets remain for RISK-420.
- GATE-290: Signed-bundle SMAppService registration cannot be proven by the SwiftPM executable and remains a packaging-gate observation.
- GATE-290: Outside-root growth is intentionally represented as unknown rather than correlated to the unmonitored fixture path.
- CONTRACT-301: MCP tool annotations are advisory client metadata, so server implementation must independently enforce the same read-only routing and closed inputs.
- CONTRACT-301: Codex and Claude configuration formats can evolve; packaged fragments require compatibility tests and explicit install-time diagnostics.
- CONTRACT-301: Session registration alone can establish only tool-linked confidence; exact writer attribution remains dependent on the optional privileged observer.
- TASK-310: Local ancestry snapshots are point-in-time; processes that exit before inspection may require the optional privileged event observer for stronger evidence.
- TASK-310: The registry is in-memory by design and must be exposed only behind the private authenticated IPC service implemented by the connector node.
- TASK-310: Workspace-only correlations are intentionally inferred, and overlapping workspaces intentionally produce unknown rather than choosing a task.
- TASK-311: macOS Unix-domain socket paths are length-limited; the product Application Support path is checked at startup and reports degraded IPC if it cannot bind.
- TASK-311: MCP cancellation is checked before and during IPC reads/writes; the current line-oriented runner processes one request at a time, so a later cancellation notification cannot preempt a server call already blocking in that same stdio process.
- TASK-311: Registered process contexts are reported as tool-linked and are not labeled proven writers; exact attribution remains unavailable until the optional privileged observer is installed.
- TASK-311: Inline evidence export uses temporary app-owned bundle files and removes each completed bundle after encoding the response.
- TASK-312: Codex and Claude may evolve their local configuration schemas; installer ownership markers and JSON key isolation make upgrades reversible, while diagnostics expose discovery failures.
- TASK-312: Task registration improves correlation but cannot prove individual file writes; the integration skill and session result preserve that limitation.
- TASK-312: The adapters depend only on macOS system zsh and Swift; the shipped app package must include or resolve the connector executable path selected during installation.
- TASK-312: Uninstall moves owned plugin/config state into a dated backup rather than destroying it, so users may later choose when to remove those small backups.
- GATE-390: Timestamp ordering at session-registration boundaries must remain explicit; events preceding registration correctly stay unknown.
- GATE-390: Endpoint Security exact provenance is intentionally outside this increment, so workspace-only task impact remains inferred.
- GATE-390: Current stdio request handling is sequential; cancellation checks bound connector IPC, but a later notification cannot preempt an already-blocked request in the same line-oriented process.
- RESEARCH-401: No valid signing identity or entitlement grant is available on this host.
- RESEARCH-401: Installed system-extension state could not be observed in the current environment.
- RESEARCH-401: Exact provenance must remain optional and unavailable across gaps or fallback-only observations.
- CONTRACT-401: Live Endpoint Security activation and event receipt remain untestable without Apple entitlement, provisioning, signing identity, Full Disk Access, and an approved test host.
- CONTRACT-401: The system-extension transport's code-signing and application-group policy must be bound to actual identifiers during release packaging.
- CONTRACT-401: Exact attribution is event-local; any sequence discontinuity, stream restart, overload, or incomplete measurement opens a visible gap and forces downgrade.
- TASK-410: A live Endpoint Security client cannot be activated or verified on this host without Apple-granted entitlement, a valid signing identity, system-extension installation, and user approval; the system runtime therefore fails closed as not-entitled while fixture transport supplies deterministic evidence.
- TASK-410: Placeholder team, bundle, and application-group identifiers must be replaced and release-bound before distributing a signed build.
- TASK-410: Any transport sequence loss, overload, restart, incomplete file identity, or missed completion deadline forces attribution below exact and keeps the fallback collector active.
- TASK-411: Presentation parity is enforced through one shared immutable record; future channel-specific serializers must continue to consume it without modifying confidence, support, or limitations.
- TASK-411: Exact claims remain event-local and require live privileged evidence; historical metadata and FSEvents hints cannot be upgraded retrospectively.
- TASK-411: Workspace correlation is intentionally conservative: concurrent matching workspaces remain unknown even when that leaves useful activity unattributed.
- RISK-420: The 100,000-event test validates deterministic queue bounds and loss accounting in the test process; production idle/load CPU and resident-memory budgets still require measurement on the signed release artifact and target macOS versions.
- RISK-420: Apple entitlement approval, release team identifiers, provisioning, activation, Full Disk Access, Developer ID signing, and notarization remain external release gates.
- RISK-420: Privacy filtering must remain upstream of persistence and every presentation channel; future fields require the same property-based leak tests before release.
- GATE-490: No live Endpoint Security event is claimed because Apple entitlement, release signing, activation approval, and Full Disk Access are unavailable on this host.
- GATE-490: The generated Swift-package DiskSteward scheme requires an explicit macOS destination from command-line Xcode; the macOS-targeted build passes.
- GATE-490: Signed-artifact idle/load CPU and resident-memory measurements, notarization, upgrade, activation, deactivation, and clean-host uninstall remain release-stage evidence.
