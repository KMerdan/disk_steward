<!-- Generated by Pyramid Task V3. Change canonical files through Pyramid Task interfaces, not this file. -->
# TASK-210: Create the canonical Xcode application bundle

## Metadata

- Kind: `implementation`
- Level: `3`
- Wave: `1`
- Workstream: `application`
- Selection: `primary`
- Execution: `implemented`
- Verification: `pending`
- Health: `at-risk`
- Availability: `implemented`
- Goal trace: `TASK-210` → `OUTCOME-210` → `OUTCOME-220` → `INTENT-002`

## Goal

Add a maintainable Xcode application product that reuses the package sources, launches as a menu-bar-only app, bundles its icon and MCP helper, and separates the optional system extension.

## Deliverables

- Canonical Xcode project and shared scheme
- Disk Steward app bundle configuration
- App icon and metadata
- Bundled MCP helper phase and smoke check

## Allowed Write Scope

- DiskSteward.xcodeproj/**
- DiskSteward.xcworkspace/**
- Config/Packaging/**
- Resources/App/**
- Scripts/Development/**
- README.md
- .gitignore
- docs/tasks/evidence/TASK-210/**

## Non-Goals

- Activating the optional Endpoint Security extension
- Notarizing a release
- Changing evidence semantics

## Dependencies

- `CONTRACT-201` (contract-requires, passed)

## Required Context

- CONTRACT-201 deliverables
- Package.swift
- Sources
- Integrations
- Extensions
- Config

## Validation Commands

- xcodebuild -project DiskSteward.xcodeproj -scheme DiskSteward -configuration Debug build
- swift test

## Acceptance Criteria

- `AC-210-01` — The shared Disk Steward scheme builds and launches a real LSUIElement application bundle from Xcode using the permanent product identity and without requiring Endpoint Security approval.
- `AC-210-02` — The product contains versioned metadata, a complete app icon, required privacy descriptions, and a bundled executable read-only MCP helper whose installed path is discoverable and smoke-tested.
- `AC-210-03` — Debug builds use automatic development signing when available and remain buildable in an explicit unsigned CI mode.

## Required Evidence

- `EVREQ-210-01` (build-and-launch-test) — xcodebuild output, bundle-content listing, Info.plist checks, launched menu-bar evidence, and MCP helper smoke transcript.

## Audit Gates

- None

