{
  "schema": "pyramid-reopen-evidence-v1",
  "target": "TASK-310",
  "discovered_by": "RISK-320",
  "finding": "The actual-artifact verifier does not yet assert the application and nested MCP bundle identifiers, so a correctly signed artifact with the wrong product identity could pass the currently implemented identity-class gates.",
  "preserved_requirement": "Wrong bundle identifiers must deterministically fail before an artifact is considered distributable.",
  "repair": "Add explicit Info.plist and nested helper identifier checks to verify-release, then rerun the release rehearsal and dependent negative tests.",
  "secrets_observed": false
}
