<!-- Generated by Pyramid Task V3.2. The JSON record is canonical. -->
# Handoff: TASK-310

## Resume Contract

- Handoff: `HANDOFF-TASK-310-20260913T085901892011Z-D75E7571`
- Plan: `PLAN-DISK-STEWARD-002` at graph version `142`
- Paused by: `codex` at `2026-09-13T08:59:01.892000Z`
- Mode: `hold`
- Resume deadline: `2026-09-13T10:59:01.892006Z`
- Reason: Waiting for the user to notarize and export the corrected archive before Homebrew publication

## Summary

Corrected the bundled MCP helper signing identity and tightened entitlement verification; paused at the user-owned Apple notarization boundary before Homebrew publication.

## Recommended First Action

In Xcode Organizer, confirm the selected archive was created around 17:53 and upload that fixed archive to the Apple notary service.

## Progress

- Diagnosed the installed notarized app: app signature, staple, and Gatekeeper passed, but the helper code-signing identifier was disk-witness-mcp instead of com.marudankiji.disksteward.mcp.
- Embedded a generated Info.plist section in the single-file helper so Xcode signs it with the canonical identifier.
- Updated the release verifier to require the helper application identifier and the exact Developer ID injected app identity, team identity, and application group.
- Built /private/tmp/DiskSteward-1.0.0-fixed.xcarchive and opened it in Xcode Organizer.
- Committed and pushed the correction as 76a8660; Homebrew activation remains intentionally blocked until the corrected archive is notarized and exported.

## Changed Files and Assets

### Files

- Config/Packaging/project.yml
- DiskSteward.xcodeproj/project.pbxproj
- Scripts/Distribution/verify-release

### Assets

- None

## Checks, Decisions, and Risks

### Checks

- `{"command": "swift test", "notes": "158 tests passed with zero failures.", "result": "passed"}`
- `{"command": "xcodebuild -project DiskSteward.xcodeproj -scheme DiskSteward-Release -configuration Release -archivePath /private/tmp/DiskSteward-1.0.0-fixed.xcarchive clean archive", "notes": "Clean universal Developer ID archive succeeded.", "result": "passed"}`
- `{"command": "codesign --verify --deep --strict --verbose=4 <fixed app>", "notes": "The enclosing app and nested helper are valid; helper identifier is com.marudankiji.disksteward.mcp with hardened runtime and secure timestamp.", "result": "passed"}`
- `{"command": "swift test --filter DistributionReleaseIntegrityTests", "notes": "Five focused distribution tests passed.", "result": "passed"}`
- `{"command": "Scripts/Distribution/verify-release /private/tmp/DiskSteward-1.0.0-fixed.xcarchive", "notes": "All pre-notarization checks pass; verifier exits 68 only because no notarization ticket is stapled yet.", "result": "partial"}`

### Decisions

- `{"decision": "Embed a generated Info.plist in the command-line helper.", "rationale": "This makes Xcode and codesign derive the canonical reverse-DNS helper identifier without manual post-signing or stored credentials.", "reference": "Config/Packaging/project.yml"}`
- `{"decision": "Keep the Homebrew cask disabled until the corrected notarized artifact exists.", "rationale": "The published cask must use an immutable release URL and SHA-256 for an artifact that passes the complete release verifier.", "reference": "docs/tasks/09-distribution/TASK-321-publish-a-fail-closed-homebrew-tap.md"}`

### Assumptions

- The user will use the newly opened fixed archive rather than the older installed notarized app.
- The release tag will be v1.0.0 once the corrected artifact passes notarization and Gatekeeper.

### Blockers

- The user must select the fixed archive in Xcode Organizer, choose Distribute App > Developer ID > Upload, wait for Ready to distribute, and export the notarized app.

### Risks

- Selecting the older archive or packaging the current /Applications copy would reintroduce the wrong helper identifier.
- Publishing before the immutable release asset exists would make the Homebrew cask unverifiable.

## Next Steps

- User notarizes and exports /private/tmp/DiskSteward-1.0.0-fixed.xcarchive.
- Run Scripts/Distribution/verify-release on the exported app and require a complete pass.
- Create a versioned zip, compute SHA-256, publish GitHub release v1.0.0, activate the cask with the immutable asset URL and checksum, push the tap, and run a clean brew install smoke test.

## Context References

- docs/tasks/08-distribution/TASK-310-build-the-credential-free-release-and-notarization-handoff.md
- docs/tasks/09-distribution/TASK-321-publish-a-fail-closed-homebrew-tap.md
- Scripts/Distribution/verify-release
- Config/Packaging/project.yml

## External Sessions and Running Resources

### External Sessions

- /private/tmp/DiskSteward-1.0.0-fixed.xcarchive
- https://github.com/KMerdan/disk_steward/commit/76a8660

### Running Resources

- `{"description": "Xcode Organizer is open with the fixed archive.", "resume_command": "open '/private/tmp/DiskSteward-1.0.0-fixed.xcarchive'", "status": "waiting for user notarization upload"}`
- `{"description": "/Applications/Disk Steward.app is the older notarized build and must not be used as the Homebrew release artifact.", "resume_command": "Scripts/Distribution/verify-release '/Applications/Disk Steward.app'", "status": "running but release-ineligible because its helper identifier is stale"}`
