<!-- Generated by Pyramid Task V3.2. The JSON record is canonical. -->
# Handoff: TASK-310

## Resume Contract

- Handoff: `HANDOFF-TASK-310-20260913T065625816516Z-45E4A49C`
- Plan: `PLAN-DISK-STEWARD-002` at graph version `123`
- Paused by: `codex` at `2026-09-13T06:56:25.816510Z`
- Mode: `handoff`
- Resume deadline: `None`
- Reason: Waiting for the user-owned Xcode Organizer notarization submission and notarized app export.

## Summary

Target-specific Developer ID profile signing is repaired and a strictly verified signed archive is open in Xcode Organizer; Apple notarization and notarized export remain user-owned.

## Recommended First Action

Complete Xcode Organizer: Distribute App > Developer ID > Upload for the already-open archive, then export the notarized app after Apple accepts it.

## Progress

- Verified the installed Disk Steward Developer ID Distribution profile targets G3P6TU385Y.com.marudankiji.disksteward and authorizes group.com.marudankiji.disksteward.
- Observed that the app needs the profile while disk-witness-mcp must remain profile-free.
- Replanned only TASK-310 write scope in plan revision 8; no nodes, edges, intent criteria, or release gates changed.
- Added target-specific Release provisioning settings to Config/Packaging/project.yml and regenerated the Xcode project and both shared schemes.
- Created the real signed archive at /Users/merdankiji/Documents/Codex/DiskSteward-Release-2026-09-13.xcarchive.
- Verified outside the restricted shell that the app and MCP helper are valid Developer ID Application signatures with hardened runtime and secure timestamps.
- Opened the archive in Xcode Organizer.

## Changed Files and Assets

### Files

- Config/Packaging/project.yml
- DiskSteward.xcodeproj/project.pbxproj
- DiskSteward.xcodeproj/xcshareddata/xcschemes/DiskSteward-Release.xcscheme
- DiskSteward.xcodeproj/xcshareddata/xcschemes/DiskSteward.xcscheme

### Assets

- None

## Checks, Decisions, and Risks

### Checks

- `{"command": "Inspect installed Disk Steward Developer ID Distribution provisioning profile", "notes": "Profile UUID 193d15aa-43f8-4477-bdab-58cf2e59964f targets the canonical app, authorizes the canonical App Group, contains the selected Developer ID Application certificate, and expires 2044-09-08. No private key was read or stored.", "result": "passed"}`
- `{"command": "xcodegen generate --spec Config/Packaging/project.yml --project-root . --project .", "notes": "Regenerated project.pbxproj plus DiskSteward and DiskSteward-Release shared schemes with app-only profile selection and an empty helper profile selector.", "result": "passed"}`
- `{"command": "Scripts/Distribution/archive-release --release /Users/merdankiji/Documents/Codex/DiskSteward-Release-2026-09-13.xcarchive", "notes": "Release preflight passed and Xcode created the archive. The only warning was a missing App Category, which does not affect signature validity.", "result": "passed"}`
- `{"command": "codesign --verify --deep --strict --verbose=4 <archived Disk Steward.app>", "notes": "Normal macOS code-signing services validated the app and nested MCP helper. A prior restricted-shell result was a sandbox/trust-service false negative and was not used as evidence.", "result": "passed"}`
- `{"command": "codesign -dv --verbose=4 <app and helper>; codesign --display --entitlements - <app>", "notes": "Both executables use Developer ID Application: Marudan Kiji (G3P6TU385Y), Team G3P6TU385Y, runtime flag 0x10000, and secure timestamps. The app contains only the expected application identifier, team identifier, and group.com.marudankiji.disksteward App Group entitlement.", "result": "passed"}`

### Decisions

- `{"decision": "Apply the provisioning profile only to the DiskSteward application target and explicitly clear it for disk-witness-mcp.", "rationale": "An application bundle can embed the profile authorizing App Groups, while the nested command-line executable cannot accept a provisioning profile and needs only its Developer ID signature.", "reference": "Config/Packaging/project.yml"}`

### Assumptions

- The user will use the Apple account already connected to Xcode Organizer to submit this exact archive for notarization.

### Blockers

- Apple notarization submission, acceptance, ticket stapling, and notarized app export have not yet occurred.

### Risks

- The archive must not be distributed until Apple accepts notarization and the exact exported app passes staple and Gatekeeper verification.
- The App Category warning should be repaired before a polished public release, although it does not invalidate the current signature or notarization test.

## Next Steps

- In Xcode Organizer select the Disk Steward archive and click Distribute App.
- Choose Developer ID, then Upload, follow Xcode's signing review, and submit to Apple.
- Wait until Xcode reports the archive is ready to distribute or otherwise shows notarization success.
- Export the notarized app to a known folder without modifying it.
- Resume TASK-310 from this canonical handoff, record the exported app path, run Scripts/Distribution/verify-release on the exact app, and finish the remaining Pyramid audits.

## Context References

- Config/Packaging/project.yml
- DiskSteward.xcodeproj/project.pbxproj
- Scripts/Distribution/archive-release
- Scripts/Distribution/verify-release
- docs/tasks/evidence/GATE-390/target-specific-profile-blocker.json
- /Users/merdankiji/Documents/Codex/DiskSteward-Release-2026-09-13.xcarchive

## External Sessions and Running Resources

### External Sessions

- Xcode Organizer with DiskSteward-Release-2026-09-13.xcarchive open

### Running Resources

- None
