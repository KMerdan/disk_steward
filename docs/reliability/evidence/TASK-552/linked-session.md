# TASK-552 — verified cursor branch transition and linked-session provenance privacy

Checkpoint: 18 September 2026 JST. Resumed from `HANDOFF-TASK-552-20260917T235923026979Z-C94A8503` at PLAN-DISK-STEWARD-005 R10 G85, task guard `GUARD-TASK-9A1675B8874041A7B92691E7E6564240`. The resume guard reported worktree drift; inspection showed only regenerated `docs/tasks/` projections, and the current product input still hashed to the handoff's `c37b843ddc2793d8169d837e6ce776cf4f62036666120df12f85537150fed8f0`, so the stale handoff was accepted after review. This is partial AC-TASK-552-01 evidence, not task completion or gate approval.

## Change and evidence chain

Both findings from `api-matrix-review-result.json` are now closed by runtime evidence bound to exact input hashes.

1. `api-matrix-branch-green/`: the unified provenance cursor envelope and revision check, unverified at handoff, ran against the corrected source. `testProvenanceCursorExpiresWhenPathBindingsChangeItsQueryBranch` passes in an isolated snapshot: 1 test, 0 failures, input `c37b843ddc2793d8169d837e6ce776cf4f62036666120df12f85537150fed8f0`. Together with `api-matrix-branch-red/` (same test, 4 failures on the pre-fix candidate) this proves both branch transitions now return nonretryable `cursor_expired`.
2. `linked-session-compile-error/`: first attempt at the new linked-session test did not compile (`await` inside an `XCTUnwrap` autoclosure). Retained as an unsuccessful attempt.
3. `linked-session-green/`: `testProvenanceLinkedSessionWithholdsTaskContextOutsideFullDetail` passes: 1 test, 0 failures, input `f85e397066ed35742cf1a6ade57c4346a42671660ee48f5bb7bd387ec0bc0b8f`. The fixture registers a session over real IPC with unstructured context naming both a workspace file and an unrelated private path, records two observations inside the session lease so the modification carries a measured occurrence interval, and lets `ProvenanceEngine.attribute` link the event to the registration from workspace/interval overlap alone. The persisted claim has no actor and `inferred` confidence; no writer evidence is invented. The test then pages `get_provenance` at limit 1 in basename, hashed and full detail until the linked event's page, and asserts a single linked session plus a claim projection naming that registration. Basename and hashed pages return `task_context: null`, `task_context_withheld: true`, and disclose neither the fixture root nor the private path; full detail returns the stored text with `task_context_withheld: false`.
4. `linked-session-mutation-red/`: a disposable snapshot with `sessionItem(detail:)` mutated to always project context (runner archived alongside) fails the same test with 8 assertions across basename and hashed, including two private-path disclosures. The repository input was unchanged before and after. This shows the new test exercises the `get_provenance` session path the review found untested, rather than passing vacuously.
5. `linked-session-focused-green/`: 48 tests across `EvidenceQueryPrivacyIPCIntegrationTests`, `EvidenceQueryReadModelTests`, `EvidenceObjectLifecycleTests`, `LifecycleProjectionIPCIntegrationTests`, `ProvenanceChronologyTests` and `TaskAttributionClaimTests` pass on input `f85e3970…`.
6. `linked-session-candidate-green/`: full isolated candidate verification — see the section below.
7. `linked-session-review-{job,result}.json`: read-only static delta review HELPER-TASK-552-LINKED-01 of the two changed files, bound to snapshot `f85e3970…` and guard `GUARD-TASK-9A1675B8874041A7B92691E7E6564240`, returned no findings: the single-envelope decode precedes branch selection in both branches, raw-branch pagination is otherwise unchanged, and both tests use real product APIs rather than stubs. Coordinator validated identity, guard, snapshot and budgets; the result stays advisory and is not runtime proof. Its one noted risk is recorded below.

The two product-file deltas versus the last full green candidate `223d3eb9adcbd3c966981869e3b3013e7f6e652be7f2714ac5c7ab9f68644052` are archived in `linked-session-source/` with hashes in `linked-session-source-manifest.json`. `EvidenceStore.swift` is byte-identical to the handoff's unverified copy; only the privacy test file changed in this checkpoint.

## Full candidate verification

Candidate `f85e397066ed35742cf1a6ade57c4346a42671660ee48f5bb7bd387ec0bc0b8f` at Git revision `cf313e45` (dirty worktree) passed all six isolated verifier stages in `/private/tmp/ds-ci-s05cck0y`: Python harness (20 tests), toolchain, package manifest, entitlements lint, release build, and the Swift suite with 499 tests, 4 opt-in skips and 0 failures. The production-sentinel fixture was preserved. Logs and `candidate.json` are archived in `linked-session-candidate-green/`. No real app, live database or client configuration was used.

## Verification boundaries

Cross-version cursors are not covered: released 1.1.0 (`cf313e4`) issued bare `EventQueryCursor` provenance cursors, and the raw branch now issues the `ProvenanceQueryCursor` envelope, so a cursor held across a live upgrade fails decoding and surfaces as the generic invalid-cursor `invalidObservation` rather than `cursor_expired`. Cursors are short-lived, revision-bound session state, but a typed nonretryable invalid-cursor mapping remains open under the AC-TASK-552-01 cursor contract.

The linked-session fixture proves the projection contract for one inferred workspace/time claim reached through pagination. It does not cover claims with observed writers, superseded claim chains, multiple linked sessions on one page, the 100-identity search bound, or session enumeration cursors. All-field redaction of diagnostics, gap reasons, claim support strings and snapshot identifiers is asserted only through the fixture's two hidden paths, not by construction.

AC-TASK-552-01 still lacks immediately effective scope/exclusion and policy changes, stale/missing evidence across every endpoint, bounded session/claim/gap expansion and the full response-budget/error matrix. AC-TASK-552-02 still needs occurrence/export composition and representative-cardinality query evidence. Scale, native-client, packaged-app, overnight and rollback gates remain unverified; no material finding or assurance gate is closed by this checkpoint. No install, signing, commit, push or release occurred.
