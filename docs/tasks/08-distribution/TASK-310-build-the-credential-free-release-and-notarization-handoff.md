<!-- Generated by Pyramid Task V3. Change canonical files through Pyramid Task interfaces, not this file. -->
# TASK-310: Build the credential-free release and notarization handoff

## Metadata

- Kind: `implementation`
- Level: `2`
- Wave: `8`
- Workstream: `distribution`
- Selection: `primary`
- Execution: `implemented`
- Verification: `passed`
- Health: `clear`
- Availability: `verified`
- Goal trace: `TASK-310` → `OUTCOME-220` → `INTENT-002`

## Goal

Provide inspectable archive, export, verification, and Xcode Organizer instructions that use Developer ID correctly while leaving account-backed final actions to the user.

## Deliverables

- Release archive configuration
- Credential-free release verification scripts
- Xcode Organizer and notarization handoff
- Missing-credential diagnostics

## Allowed Write Scope

- Config/Packaging/project.yml
- Config/Signing/**
- DiskSteward.xcodeproj/project.pbxproj
- DiskSteward.xcodeproj/xcshareddata/xcschemes/**
- Config/ExportOptions/**
- Scripts/Distribution/**
- docs/release/**
- docs/tasks/evidence/TASK-310/**

## Non-Goals

- Saving notarization credentials
- Automatically manipulating certificates
- Requiring Endpoint Security for the standard release

## Dependencies

- `RESEARCH-201` (requires, passed)

## Required Context

- RESEARCH-201 readiness record
- DiskSteward.xcodeproj
- Config/Signing
- Config/Entitlements
- Scripts/Distribution
- Exported notarized app showing the single-file MCP helper identifier mismatch
- Current exported and notarized application bundle evidence

## Validation Commands

- xcodebuild archive
- codesign --verify --deep --strict --verbose=2
- spctl --assess --type execute --verbose=4
- xcrun stapler validate

## Acceptance Criteria

- `AC-310-01` — A release scheme archives the app with hardened runtime and production entitlements, and documented Xcode Organizer steps guide Developer ID distribution and notarization without embedding account credentials.
- `AC-310-02` — Release scripts accept an archive or exported artifact, verify expected identity class, the single-file MCP helper code-signing identity and application-identifier entitlement, entitlements, timestamp, notarization and staple status, and Gatekeeper acceptance, and reject ad hoc or Apple Distribution-only output for direct release.
- `AC-310-03` — When Developer ID access is missing, the workflow stops with the precise Xcode or account-holder action and leaves a usable development build intact.

## Required Evidence

- `EVREQ-310-01` (release-workflow-test) — Archive logs, expected-failure fixtures, signed-artifact inspection when available, and a successful human handoff rehearsal.

## Audit Gates

- None

